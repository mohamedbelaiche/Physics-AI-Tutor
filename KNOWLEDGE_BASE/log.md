# السجل الزمني لقاعدة المعرفة (log — append-only)

> اصطلاح صارم: كل سطر يبدأ بـ `## [YYYY-MM-DD] <op> | <الوصف>`.
> التنقّل: `grep "^## \[" KNOWLEDGE_BASE/log.md | tail -5`.

## [2026-09-18] ingest | تأسيس نمط LLM Wiki: ملف الـ schema (AGENTS.md) وقواعد الطبقات الثلاث والعمليات (Ingest/Query/Lint)
## [2026-09-18] ingest | توليد أول فهرس محتوى `KNOWLEDGE_BASE/index.md` آليًا من `manifest.json` (271 سجلًا — دروس 5، مفاهيم 34، صيغ 36، تمارين 117، حلول 79)
## [2026-09-18] tools | توجيه الدماغ إلى الويكي: إعادة بناء `api/context.js` ليقرأ من صفحات KNOWLEDGE_BASE (دروس + مفاهيم/صيغ مُختارة حسب السؤال) بدل المصادر الخام، وتمرير نص السؤال من `api/chat.js`
## [2026-09-23] tools | بناء واجهة "الكورسات": خمسة كورسات انبثقت من صفحات الدروس الـ5 في الويكي — أدوات: `scripts/build-course-content.js` (يولّد `public/course/course.json` من دروس الويكي), `scripts/smoke-course.js` (فحص: 5 كورسات/60 مقطعًا OK), `public/course.js` + أنماط في `public/style.css` (لوحة الكورسات + عرض الدرس بـ marked + KaTeX). النسخة الأولى كانت تسجّل التقدم في `course_progress` بـ Supabase (هجرة 018) — ثم أُلغي ذلك بناءً على قرار المالك.
## [2026-09-23] tools | قرار المالك: لا تصدير للبيانات إلى أي قاعدة بيانات خارجية — أُعيد بناء متابعة تقدم الكورسات لتكون محلية في المتصفح فقط: حُذف `api/course-progress.js` ومساره من `server.js`، حُذفت هجرة `018_course_progress.sql` وأُسقط جدول `course_progress` من قاعدة البيانات، وأصبحت الحالة تُحفظ في `localStorage` (مفتاح `physics_course_progress_v1`) بلا تسجيل دخول. تم تحديث الخطة الشاملة وملف الـ agent و`كيفية_بناء_agent_الكورسات` ليعكسان القرار.
## [2026-09-23] tools | تراجع المالك عن سطر «لا Supabase» السابق وأعيد تفعيل النسخة الأصلية كاملة: `api/course-progress.js` أُعيد (GET/POST مع فرض التسلسل خادمًا + `setSession` لحصر RLS بالطالب)، أُعيدت هجرة `018_course_progress.sql` وطُبّقت على قاعدة البيانات (جدول `course_progress` + 4 سياسات RLS)، أُعيد ربط المسار في `server.js`، وأُعيدت الواجهة (تبويبات + بطاقات + إتمام بشرط تسجيل الدخول) في `public/course.js` و`index.html`. استُعيدت الخطة الشاملة/ملف الـ agent/`كيفية_بناء_agent_الكورسات` إلى صيغتها القائمة على Supabase.
## [2026-09-23] tools | طبقة تأليف الشرائح التفاعلية (أمر /build-slides): contract في تعليمات بناء course/الخطة_الشاملة_للشرائح_التفاعلية.md، وكلاء slides-director + 4 وكلاء فرعيين في .opencode/agents/، أمر /build-slides، مشغل public/slides-player.js + public/md-render.js، ربط مبدّل النص/الشرائح في public/course.js + index.html + style.css، فاحص scripts/smoke-slides.js (أوامر check:slides)، تجربة unit_01_chemical_kinetics كاملة (13 deck + 8 script مشهد + 8 SVG + index.json + summary.md) في KNOWLEDGE_BASE/slides/ و public/course/slides/.

## [2026-09-23] tools | إصلاح جذر خطأ «permission denied for table course_progress» في api/course-progress.js: كان يعتمد على auth.supabase.auth.setSession({access_token,refresh_token:''}) الذي يفشل بصمت في supabase-js v2 (refresh_token فارغ → الجلسة لا تُفعَّل) فتذهب الطلبات بدور anon فتُرفض من RLS. أُعيد بناؤه بنمط chat.js المثبت: استدعاء PostgREST مباشرةً برأس apikey + Authorization: Bearer <JWT المستخدم الفعلي> (وظيفة postgrestHeaders) — GET تسحب الصفوف بدور authenticated، POST تُدرج بـ on_conflict=user_id,course_id وPrefer=resolution=merge-duplicates، وتبقى RLS/الـ grants والهجرة 018 كما هي (صحيحة). تم التحقق: node --check سليم.
## [2026-09-23] tools | نظام تتبع المقاطع والشرائح + استئناف الموقع داخل كل كورس (قرار المالك: «مقاطع + أجزاء لكل كورس»، خادم + localStorage): هجرة 020 أنشأت جدول `course_positions` (PK user_id,course_id؛ section_id + slide_index + mode؛ RLS للأصالة) وسجّلت الأجزاء في `section_progress` (هجرة 019، جزء لكل شريحة أو «text»). `api/course-progress.js`: GET يرفق `positions` كخريطة، وPOST يقبل `part_key` اختياريًا مع تحديث الموقع. `public/course.js`: حفظ الموقع عند التنقّل/تبدّيل الوضع/مغادرة الصفحة (localStorage دائمًا، خادم مخفَّف للمسجّل)، استئناف القسم+الشريحة+الوضع عند فتح الكورس، وشريط أقسام (لوحة تقدم داخلية) قابلة للتنقّل بين المقاطع مع علامات مكتمل/ن-من-إجمالي. `public/slides-player.js`: `playSection` تقبل شريحة بداية غير صفرية + `getPos()` وكشف slideIdx في معالج التقدم. تحقق: node --check، check:course/check:slides، وharness شامل (401/GET/positions/POST بلا course_id/كورس مجهول/إكمال جزء/استمرارية الموقع) — كلها نجحت (تسجيل دخول حقيقي مؤقت ثم حُذف المستخدم).
## [2026-09-25] tools | توسيع عرض شرائح الكورس + زر ملء الشاشة: `public/index.html` أضاف `#lesson-presentation-controls` (غلاف مبدّل الوضع) و`#slides-fullscreen-btn`؛ `public/course.js` أضاف `updateSlidesFullscreenUI`/`exitLessonFullscreen`/`leaveLessonSlides`/`toggleSlidesFullscreen`/`supportsLessonFullscreen`، ووسّع `setModeUI` (فئة `body.slides-focus` + `aria-selected`)، وحوّل مسار `switchMode` إلى غير متزامن مع مسار رفض وfallback، وصحّح تسرّب الحالة (إتمام الكورس، تبديل التبويبات، عدم توفّر الـdeck مع إجبار الحفظ لتجاوز تخفيف 2s)، و`showSuccess` ينهي وضع الشرائح ويفتح الكورس التالي عبر `openCourse`؛ `public/style.css` أضاف توسيع `main`، وأنماط الزر، وقواعد `:fullscreen` مع متغيّر `--lesson-chrome` (تكبير المشاهد يقتصر على العرض الموسّع حفاظًا على تخطيط الشرائح العادي)؛ `scripts/course-slides-ui.test.js` جديد (harness بـvm يحمّل `course.js`+`slides-player.js` الحقيقيين، 20 اختبارًا مربوطًا بـ`npm test` مع تحقّق من ثوابت الـids في `index.html`)، و`package.json` أضاف `test`. تحقّق: node --check، check:course، check:slides، npm test — كلها ناجحة.
## [2026-09-25] tools | تصحيح «ملء الشاشة» إلى توسيع داخل مساحة الموقع (قرار المالك: المقصود أن الشريحة تشمل مساحة الموقع بالكامل، لا وضع ملء الشاشة الأصلي للمتصفح): حُذف كل اعتماد على `requestFullscreen`/`exitFullscreen`/`fullscreenchange`/`fullscreenerror` من `public/course.js` واستُبدل بـ`setSlidesExpand(on)`/`toggleSlidesExpand()` على صنف واحد `body.slides-focus` (يبدأ غير مفعّل، فتبقى الشرائح بحجمها المعتاد داخل الصفحة)، وأُعيدت تسمية `#slides-fullscreen-btn` ← `#slides-expand-btn` مع `aria-label` متبدّل («توسيع/تصغير عرض الشرائح») و`aria-pressed`؛ `setModeUI` صارت تعرض الزر متى كان الوضع شرائح ومتاحًا بلا فحص دعم، وتُسقِط التوسيع تلقائيًا عند التبديل إلى النص أو مغادرة التبويب أو الدرس أو إتمام الكورس (مع إبقاء وضع الشرائح وموضعه)؛ `public/style.css` حُذفت منه كتل `:fullscreen` و`--lesson-chrome` الميتة، وأُضيف وضع موسّع يُخفي `header.site-header` و`nav.view-tabs` و`site-footer` و`.chat-panel` و`#course-lesson-back` و`#course-lesson-head` و`#course-sections-bar`، ويمرّر `main` بلا حشو مع `100dvh`، ويجعل `#lesson-presentation-controls` عائمًا بـ`position: fixed` بتدرّج خلفي، ويكبّر `.slides-scene-img` إلى `100dvh - 14rem`؛ `scripts/course-slides-ui.test.js` حُذف من هيكله بدائل Fullscreen API (الخيارات `fullscreenSupported`/`fullscreenRequestFails`/`fullscreenRequestRejects`/`exitFullscreenRejects` و`isFullscreen`) فصار يعمل في سياق بلا `requestFullscreen` أصلًا، وأُعيدت كتابة 6 اختبارات إلى اختبارات توسيع/انهار/غياب-API-أصلي (الآن 16 اختبارًا). تحقّق: node --check لـ`course.js` و`slides-player.js`، npm test (16/16)، check:course (5 كورسات، 60 مقطعًا)، check:slides (1 deck، 41 شريحة، 8 مشاهد) — كلها ناجحة.
## [2026-09-25] tools | مساحة آمنة متناسبة للشرائح (إصلاح «النص يظهر في الحواف»): السبب الجذري في `d60a5fd` — وضع التوسيع كان يصفّر الحشوة على `#courses-group`/`#course-lesson-view`/`.course-content.slides-mode` مع `max-width: none` على `main`، فحُذفت طبقات الاحتواء الثلاث معًا ولم يبقَ بديل، فامتدّ عمود النص على كامل عرض الشاشة (≈1920px، سطر يتجاوز 200 محرف) بلا مسافة من الحواف. الإصلاح: `.slides-wrap` صار حاوية المساحة الآمنة في الوضعين بمتغيّرات متناسبة `--slide-safe-inline: clamp(1rem, 4vw, 4rem)` و`--slide-safe-block: clamp(4.5rem, 8vh, 7rem)` (تناسب كل أحجام الشاشات: جوال/حاسب/شاشة كبيرة) مع `max-width: min(1200px, 100%)` و`margin-inline: auto` و`box-sizing: border-box`، ونُزع `padding: 0` من الحاوية الموسّعة؛ `.slides-text` أخذ `max-width: 68ch` (قِيس قراءة يتبع عرض المحرف وحجم الخط والعربية فلا يطول مع كبر الشاشة) مع `line-height: 1.9`؛ المشاهد صارت تتوقف عند `calc(100dvh - (2 * var(--slide-safe-block) + 6rem))`؛ والبطاقة الموسّعة `overflow-y: auto` بعرض كامل الارتفاع حتى يُمرَّر المحتوى الأطول داخلها بدل قصّه. `scripts/course-slides-ui.test.js`: أُضيف مساعد `cssBlock(css, selector)` (مُحدَّد مثبَّت في بداية السطر) و3 اختبارات انحدار تقرأ `public/style.css` وتثبّت العقد (تعريف المتغيّرات النسبية + عمود `min(px, 100%)` متمركز، عدم وجود `padding: 0`/`max-width: none` على حاوية الوضع الموسّع، قِيس `68ch` و`line-height` وحدّ المشاهد) — 19 اختبارًا. TDD: 3 فشلات قبل التنفيذ ثم 19/19. تحقّق: node --check، npm test (19/19)، check:course، check:slides — كلها ناجحة.
## [2026-09-25] tools | زر «التالي» في الشريحة الموسّعة: لا يُعثر عليه بالنزول، ولا يركب على الشريحة (قرار المالك: الزر ثابت لكن موضعه لا يغطّي العرض أو المعلومات): التشخيص — سلسلة السياق `body{overflow:hidden} ← #course-lesson-body{min-height:100dvh;display:flex;column} ← .slides-wrap{flex:1;min-height:100dvh;overflow-y:auto}` تُبقي شريط `.slides-nav` (وهو آخر عنصر في البطاقة) تحت حافة الشاشة حين يتجاوز محتوى الشريحة المساحة الآمنة، فلا يظهر إلا بعد النزول، والصفحة نفسها ممنوعة من التمرير فلا يوجد مِرْيار احتياطي وأثر بصري يدل على وجود محتوى أسفل — ولهذا كان العطل «أحيانًا»: يظهر مع الشرائح القصيرة ويختفي مع الطويلة. وقد استُبعد فرضية `min-height:0` بعد الحساب: `flex:1 1 0%` مع `min-height:100dvh` يجعل البطاقة `100dvh` بالضبط فيعمل `overflow-y:auto` فعلًا، وإضافة `min-height:0` كانت ستهدم `.slides-body` وتُداخل نصّه مع الزر. الإصلاح: قاعدة `body.slides-focus .slides-nav` بـ`position: sticky; bottom: 0` (اختير sticky لا fixed/absolute مقصودًا: العنصر الملتصق يبقى **ضمن التدفّق** فيحجز مكانه فلا يغطّي المشهد ولا نصّ الشريحة، ومع ذلك يبقى الزر نغازًا مهما طال المحتوى) مع `background: #fff` بلون البطاقة و`box-shadow: 0 -1px 0 #e5ebf1` و`padding-block: 0.6rem` حتى لا يظهر النص متباخلًا مع الأزرار؛ و`scrollbar-gutter: stable` + `overscroll-behavior: contain` على البطاقة يُعلنان وجود محتوى أسفل ويمنعان اهتزاز التمرير؛ وحدّ المشاهد `calc(100dvh - (2*var(--slide-safe-block) + 6rem))` يبقى يحجز مكان الشريط فلا ينزل المشهد تحته. `scripts/course-slides-ui.test.js`: اختبار انحدار جديد يقرأ `public/style.css` ويثبّت العقد (sticky + `bottom:0` + منع fixed/absolute + خلفية معتمة غير `none` + `scrollbar-gutter` على المِرْيار + `max-height:calc()` للمشهد) — 20 اختبارًا. TDD: فشل واحد قبل التنفيذ ثم 20/20. تحقّق: node --check، npm test (20/20)، check:course، check:slides — كلها ناجحة.
## [2026-09-25] tools | شرائح تفاعلية للكورس الثاني «التحويلات النووية» (unit_02_nuclear_transformations) عبر خط الإنتاج المعتمد /build-slides (المراحل ①②③④ بالوكلاء الأربعة): 9 أقسام و35 شريحة و9 مشاهد SVG. المخرجات: KNOWLEDGE_BASE/slides/unit_02_nuclear_transformations/sections/01..09.deck.md (نص التلاميذ) + scenes/9×*.script.json (السيناريوهات) + summary.md، وpublic/course/slides/unit_02_nuclear_transformations/assets/9×*.svg (SMIL، viewBox 640×360، role=img، reduced-motion، بلا JS) + index.json (9 sections/35 slides/9 scene، layout two-col للمشاهد، عناوين الأقسام من course.json). المشاهد التسعة: α (s2-sl2) · β⁻ (s2-sl4) · مخطط N–Z (s2-sl10) · منحنى التناقص (s3-sl2) · A(t) مقابل N(t) (s4-sl3) · تأريخ رمزي (s5-sl3) · Δm وطاقة الربط (s6-sl4) · انشطار (s7-sl1) · اندماج (s7-sl2). قرارات: لا uild:slides (أصول ثابتة مولّدة يدويًا، لا سلسلة بناء)؛ لا تعديل على المصادر الخام ولا المشغّل ولا CSS؛ التأريخ الإشعاعي رمزي 100% (لا عمر نصف ولا نسبة ولا Bq)، وعمودا s6-sl4 رمزيان (لا MeV ولا Δm رقمي) امتثالًا لملاحظات السيناريو؛ اتجاه نطاق الاستقرار في s2-sl10 اتُّبع نص الدرس حرفيًا. تحقّق مستقل: 9/9 سكربتات JSON صالحة وبلا هدف حركة معلّق · 9/9 SVG صالحة XML و viewBox صحيح و role=img و reduced-motion و لوحة ألوان فقط و كل عنصر معلَن مرسوم و صفر قيمة فيزيائية مخترعة · index.json: 9 أقسام مطابقة لـ course.json حرفيًا، 35 شريحة، 9 مشهد، 0 معلّق/0 يتيم · check:slides (2 decks، 76 شريحة، 17 مشهد) · check:course (5 كورسات، 60 مقطعًا) · npm test (20/20) · build-knowledge-base.js — كلها ناجحة. git status: مجلّدان جديدان غير متتبَّعين فقط، صفر تعديل على أي ملف متتبَّع.
